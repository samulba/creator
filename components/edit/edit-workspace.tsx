const sections = ["Hook", "First Chase", "Teammate Fail", "Second Chase", "Endgame", "Escape"];
export function EditWorkspace() { return <div className="space-y-5"><div className="rounded-xl border border-white/8 bg-white/[0.025] p-5"><h3 className="font-semibold">Intelligent edit</h3><p className="mt-2 text-sm text-stone-400">High-level creative controls without a complex editing timeline.</p><div className="mt-5 grid gap-3 md:grid-cols-3">{sections.map((section, index) => <button key={section} className={`rounded-lg border p-4 text-left ${index === 2 ? "border-white/8 bg-black/20 text-stone-500" : "border-sky-400/20 bg-sky-400/[0.04] text-stone-100"}`}><p className="text-sm font-medium">{section}</p><p className="mt-2 text-xs">{index === 2 ? "Excluded" : "Included"}</p></button>)}</div></div><div className="grid gap-3 md:grid-cols-3">{["Pacing: Tight", "Preserve gameplay: High", "Narration density: Balanced"].map((control) => <button key={control} className="rounded-lg border border-white/8 bg-white/[0.025] p-4 text-left text-sm hover:bg-white/[0.05]">{control}</button>)}</div></div>; }
